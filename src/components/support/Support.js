import { Typography, Box } from '@mui/material';

const Support = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Support
      </Typography>
      <Typography>Support center coming soon...</Typography>
    </Box>
  );
};

export default Support; 